---
const quotes = [
  { text: "El escritor debe escribir para ser leído.", author: "Jean-Paul Sartre" },
  { text: "Me gustaría pasar el resto de mis días con alguien que no me necesite para nada, pero me quiera para todo.", author: "Mario Benedetti" },
  { text: "Querer a las personas como se quiere a un gato, con su carácter y su independencia, sin intentar domarlo, sin intentar cambiarlo, dejarlo que se acerque cuando quiera, siendo feliz con su felicidad.", author: "Julio Cortázar" }
];
---

<div class="flex flex-col gap-4">
    <h2 class="text-2xl text-leg2 font-semibold font-ubuntu-c">Mural de frases</h2>
    <div class='flex flex-col gap-10 font-ubuntu'>
        {quotes.map((quote, index) => (
            <p class={`text-leg3 col-span-${index === quotes.length - 1 ? "3" : "1"} italic text-center rounded-xl`}>
                "{quote.text}"
                <br>
                — <strong class='text-leg2'>{quote.author}</strong>.
            </p>
        ))}
    </div>
</div>
